PORT=5000
# MongoDB connection URI (Atlas string or local MongoDB instance)
MONGO_URI=mongodb://localhost:27017/disciplinex

# JSON Web Token sign secret key
JWT_SECRET=your_jwt_signing_secret_here

# Mode of deployment (development / production)
NODE_ENV=development
